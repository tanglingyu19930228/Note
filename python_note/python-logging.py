import logging
logging.basicConfig(level=logging.DEBUG,
                    format='%(asctime)s - %(levelname)s: %(message)s',
                    filename='abc.txt',
                    filemode='w')
#x=0
#if x==0:
logging.debug('sds')
logging.warning('sdsd')